# ── Discord ──────────────────────────────────────────
# Channel where all team bots operate
WORK_CHANNEL_ID=your_channel_id

# ── Discord Bot Tokens (one per team) ───────────────
# Create a separate Discord Application + Bot for each team
# See README.md for step-by-step instructions
LEADER_DISCORD_TOKEN=your_leader_bot_token
PLANNING_DISCORD_TOKEN=your_planning_bot_token
BACKEND_DISCORD_TOKEN=your_backend_bot_token
FRONTEND_DISCORD_TOKEN=your_frontend_bot_token
REVIEW_DISCORD_TOKEN=your_review_bot_token
DESIGN_DISCORD_TOKEN=your_design_bot_token

# ── GitHub Tokens (one per team that pushes/creates PRs) ──
# Create a separate GitHub account for each team
# Generate a Personal Access Token (PAT) for each: Settings → Developer settings → PAT
# Only teams that push code need a token (e.g., Review)
# Other teams can share a token or leave blank
LEADER_GITHUB_TOKEN=
PLANNING_GITHUB_TOKEN=
BACKEND_GITHUB_TOKEN=your_backend_github_pat
FRONTEND_GITHUB_TOKEN=your_frontend_github_pat
REVIEW_GITHUB_TOKEN=your_review_github_pat
DESIGN_GITHUB_TOKEN=

# ── Hook Server ─────────────────────────────────────
HOOK_PORT=9876
